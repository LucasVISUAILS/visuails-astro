---
// VISUAILS — /start/custom-look. See the head of HoldingPage.astro, and the
// note at its 'custom-look' entry for why this page exists at all: a custom
// look has no standard rate, so it cannot sit in a form that prices itself.
import Layout from '../../layouts/Layout.astro';
import HoldingPage from '../../components/order/HoldingPage.astro';
---

<Layout
  title="A custom look — VISUAILS"
  description="A lifestyle world designed around your product rather than one of the four house styles. Priced per request, in writing, before anything runs."
>
  <HoldingPage lang="en" kind="custom-look" />
</Layout>
