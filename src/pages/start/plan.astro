---
// VISUAILS — /start/plan. A placeholder that says so.
// The page body is HoldingPage.astro, shared with the other two services that
// have no checkout yet, so the three read as one deliberate category.
import Layout from '../../layouts/Layout.astro';
import HoldingPage from '../../components/order/HoldingPage.astro';
---

<Layout title="Ask about a monthly plan — VISUAILS" description="Reserved capacity every month, below the per-product rate. Plans are agreed in writing first; this is where that starts.">
  <HoldingPage lang="en" kind="plan" />
</Layout>
