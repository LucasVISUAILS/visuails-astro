---
// VISUAILS — /start/brand-model.astro. The Brand Model brief.
//
// This was a HoldingPage saying "no checkout yet". It is a real order now, and
// the reason it is a BRIEF rather than a checkout is Lucas's own: a Brand Model
// is commissioned, not configured, so the first step is eight questions and the
// second is a conversation. The body lives in BrandModelBrief.astro so the two
// locales cannot drift; only the meta differs here.
import Layout from '../../layouts/Layout.astro';
import BrandModelBrief from '../../components/BrandModelBrief.astro';
---

<Layout title="Request a Brand Model — VISUAILS" description="One face, made only for you. It starts with a few short questions and a conversation about who should wear it — not a checkout button.">
  <BrandModelBrief lang="en" />
</Layout>
