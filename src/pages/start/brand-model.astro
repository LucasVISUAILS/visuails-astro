---
// VISUAILS — /start/brand-model. A placeholder that says so.
// The page body is HoldingPage.astro, shared with the other two services that
// have no checkout yet, so the three read as one deliberate category.
import Layout from '../../layouts/Layout.astro';
import HoldingPage from '../../components/order/HoldingPage.astro';
---

<Layout title="Request a Brand Model — VISUAILS" description="One face, made only for you, one-time. There is no checkout for it: it is built from a brief, so it starts with a conversation.">
  <HoldingPage lang="en" kind="brand-model" />
</Layout>
