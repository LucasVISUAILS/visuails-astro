---
// VISUAILS — /start/video. A placeholder that says so.
// The page body is HoldingPage.astro, shared with the other two services that
// have no checkout yet, so the three read as one deliberate category.
import Layout from '../../layouts/Layout.astro';
import HoldingPage from '../../components/order/HoldingPage.astro';
---

<Layout title="Video clips — VISUAILS" description="The video order flow is not finished. Clips are produced by hand today — say what you need and the studio answers in writing.">
  <HoldingPage lang="en" kind="video" />
</Layout>
