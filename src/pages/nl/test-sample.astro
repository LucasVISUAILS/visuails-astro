---
// VISUAILS — test sample. Ported from
// /tmp/visuails-rebuild/src/routes/test-sample/+page.svelte: a 2-step
// request wizard (style/model/product -> contact) inside one <form>.
//
// The Svelte source used $state(step) with class:hidden-step to keep both
// steps mounted so field values survive stepping back and forth. This is a
// static Astro page (no client framework state), so the step-switching is
// reimplemented as vanilla JS below, keeping the exact same markup/fields
// and the same "both steps stay in the DOM" behavior.
import Layout from '../../layouts/Layout.astro';

const styles = [
  { value: 'Phone-made', photo: '/img/lifestyle-phone-made-01.webp' },
  { value: 'Glow', photo: '/img/lifestyle-glow-01.webp' },
  { value: 'Flash', photo: '/img/lifestyle-flash-01.webp' },
  { value: 'Dunes', photo: '/img/lifestyle-dunes-01.webp' },
];
const roster = ['Aaron', 'Ava', 'Dana', 'Elias', 'Fabi', 'Lisa', 'Maegan', 'Rae', 'Ryan', 'Seme'];
---

<Layout
  title="Test VISUAILS met 1 product — proefvisual"
  description="Upload één productfoto en laat VISUAILS er een professionele e-commerce visual van maken. Zie hoe het werkt voordat je bestelt — 1 proefvisual per bedrijf, met de hand gecontroleerd."
>
  <!-- HERO -->
  <section class="page-hero"><div class="container">
    <span class="eyebrow-page">Proefvisual</span>
    <h1 class="display" style="font-size:clamp(2.4rem,5vw,4rem)">Test VISUAILS met <span class="accent-word">1 product</span></h1>
    <p class="lead" style="margin-top:1.2rem">Upload één productfoto en zie hoe die een publicatieklare visual wordt — voordat je bestelt.</p>
    <div class="chip-row" style="margin-top:1.8rem">
      <span class="chip"><span class="dot"></span>1 proef per bedrijf</span>
      <span class="chip"><span class="dot"></span>Slechts €0,99 verificatie</span>
      <span class="chip"><span class="dot"></span>Met de hand gecontroleerd</span>
    </div>
  </div></section>

  <!-- WHAT YOU GET -->
  <section class="section-tight">
    <div class="container two-col">
      <div class="reveal pending">
        <h2>Eén product, omgezet in een publicatieklare visual.</h2>
        <p style="margin-top:1.1rem;color:var(--ink-3);max-width:52ch">Dezelfde pipeline en menselijke controle die we bij elke betaalde bestelling gebruiken — de eerlijke manier om de kwaliteit te beoordelen voordat je je vastlegt.</p>
        <ul class="checklist" style="margin-top:1.4rem">
          <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Eén visual in hoge resolutie, geleverd via een downloadlink per e-mail.</span></li>
          <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Een echte blik op onze belichting, kadrering en afwerking.</span></li>
          <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Door een persoon gecontroleerd voordat het je bereikt.</span></li>
          <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Geen abonnement — alleen een verificatie van €0,99, 1 proef per bedrijf.</span></li>
        </ul>
      </div>
      <div class="reveal pending">
        <div class="photo-band">
          <img src="/img/lifestyle-glow-03.webp" alt="Voorbeeld van een VISUAILS-visual, Glow-stijl" loading="lazy" />
          <div class="scrim"></div>
          <div class="caption"><p style="margin:0">Jouw proef, met de hand gecontroleerd</p></div>
        </div>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section>
    <div class="container">
      <div class="section-head">
        <h2>Hoe de proef werkt</h2>
        <p>Vier korte stappen van jouw foto tot een afgewerkte visual in je inbox.</p>
      </div>
      <div class="steps">
        <div class="step reveal pending"><div class="step-n">01</div><h3>Upload productfoto</h3><p>Deel één duidelijke foto van het product waarmee je ons wilt laten werken.</p></div>
        <div class="step reveal pending"><div class="step-n">02</div><h3>Kies een stijl</h3><p>Kies de look en, als je wilt, een model — of laat ons kiezen.</p></div>
        <div class="step reveal pending"><div class="step-n">03</div><h3>Wij maken het</h3><p>Geproduceerd in onze studio, daarna met de hand gecontroleerd.</p></div>
        <div class="step reveal pending"><div class="step-n">04</div><h3>Ontvang per e-mail</h3><p>Je afgewerkte visual komt binnen als downloadlink, meestal binnen ~24 uur.</p></div>
      </div>
    </div>
  </section>

  <!-- INPUT GUIDANCE -->
  <section class="section-tight">
    <div class="container">
      <div class="section-head">
        <h2>Geef ons een goede foto, krijg een geweldig resultaat</h2>
        <p>Hoe duidelijker je input, hoe beter de visual die we ervan kunnen bouwen.</p>
      </div>
      <div class="grid grid-2">
        <div class="card reveal pending">
          <h3 style="color:var(--success)">Doen</h3>
          <ul class="checklist" style="margin-top:1rem">
            <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Helder, scherp en in focus</span></li>
            <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Neutrale, rustige achtergrond</span></li>
            <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Goede, gelijkmatige belichting</span></li>
            <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Het hele product in beeld</span></li>
          </ul>
        </div>
        <div class="card reveal pending">
          <h3 style="color:var(--ink-3)">Niet doen</h3>
          <ul class="checklist" style="margin-top:1rem">
            <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--ink-3)"><path d="M18 6L6 18M6 6l12 12" /></svg><span>Wazige of laag-resolutie foto's</span></li>
            <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--ink-3)"><path d="M18 6L6 18M6 6l12 12" /></svg><span>Donkere of sterk beschaduwde foto's</span></li>
            <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--ink-3)"><path d="M18 6L6 18M6 6l12 12" /></svg><span>Drukke of afleidende achtergronden</span></li>
            <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--ink-3)"><path d="M18 6L6 18M6 6l12 12" /></svg><span>Zware filters of watermerken</span></li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- REQUEST WIZARD -->
  <section>
    <div class="container narrow">
      <div class="section-head">
        <h2>Vraag je proefvisual aan</h2>
        <p>Twee snelle stappen. Vertel ons de look die je wilt, daarna waar we die naartoe sturen.</p>
      </div>
      <div class="card" style="padding:clamp(1.4rem,3vw,2.4rem)">
        <!-- Static site, no backend: this form needs a real submit handler (e.g. a Cloudflare Pages Function, Formspree, or similar) wired up before launch. -->
        <form action="/nl/thank-you" method="get" data-wizard novalidate>
          <div class="progress" aria-hidden="true"><i data-wizard-progress style="width:50%"></i></div>

          <!-- STEP 1 -->
          <div data-wizard-step="1">
            <h3 style="margin-bottom:.4rem">Stap 1 &middot; Stijl, model &amp; product</h3>
            <p class="hint" style="margin-bottom:1.4rem">Kies de look die je zoekt en upload je productfoto.</p>

            <div class="field">
              <label>Kies een stijl <span class="req">*</span></label>
              <div class="choice-grid">
                {styles.map((s, i) => (
                  <label class="choice">
                    <input type="radio" name="style" value={s.value} required={i === 0} />
                    <div class="choice-media"><img src={s.photo} alt={`${s.value} lifestylestijl`} loading="lazy" /></div>
                    <div class="choice-label">{s.value}</div>
                  </label>
                ))}
              </div>
            </div>

            <div class="field">
              <label>Kies een model</label>
              <span class="hint" style="margin-bottom:.3rem">Optioneel voor de meeste producten — handig voor kleding en accessoires.</span>
              <div class="choice-grid">
                {roster.map((m) => (
                  <label class="choice">
                    <input type="radio" name="model" value={m} />
                    <div class="choice-media"><img src={`/img/model-${m.toLowerCase()}.webp`} alt={m} loading="lazy" /></div>
                    <div class="choice-label">{m}</div>
                  </label>
                ))}
                <label class="choice">
                  <input type="radio" name="model" value="VISUAILS choose" />
                  <div class="choice-media mono"><div class="mono-av"><svg viewBox="0 0 24 24" class="i" style="width:26px;height:26px;color:var(--accent-bright)"><path d="M12 2l2.4 7.4H22l-6 4.6 2.3 7.4-6.3-4.6L5.7 21.4 8 14 2 9.4h7.6z" /></svg></div></div>
                  <div class="choice-label">Laat VISUAILS kiezen</div>
                </label>
              </div>
            </div>

            <div class="field">
              <label for="ts-upload">Upload productfoto's <span class="req">*</span></label>
              <label class="dropzone" for="ts-upload">
                <svg viewBox="0 0 24 24" class="i"><path d="M12 16V4M6 10l6-6 6 6M4 20h16" /></svg>
                <div><strong>Klik om te uploaden</strong> of sleep foto's hierheen</div>
                <div class="hint" style="margin-top:.4rem">Eén duidelijke foto volstaat — maar voor-, achterkant, een detail en de stof geven het beste resultaat.</div>
                <input type="file" id="ts-upload" name="photo" accept="image/*" multiple hidden />
              </label>
              <span class="hint">Meer hoeken helpen ons je product precies te matchen — zie <a href="/nl/upload-guidelines" style="color:var(--accent-bright)">wat je moet sturen</a>.</span>
            </div>

            <div class="field">
              <label for="ts-notes">Nog iets wat we moeten weten?</label>
              <textarea class="textarea" id="ts-notes" name="notes" placeholder="Optioneel — kleuren die kloppend moeten blijven, een hoek die je verkiest, iets specifieks over het product."></textarea>
            </div>

            <div class="flex" style="justify-content:flex-end;margin-top:.4rem">
              <button type="button" class="btn btn-primary" data-wizard-next>Volgende
                <svg viewBox="0 0 24 24" class="i"><path d="M5 12h14M13 6l6 6-6 6" /></svg>
              </button>
            </div>
          </div>

          <!-- STEP 2 -->
          <div data-wizard-step="2" class="hidden-step">
            <h3 style="margin-bottom:.4rem">Stap 2 &middot; Details &amp; contact</h3>
            <p class="hint" style="margin-bottom:1.4rem">Waar zullen we je afgewerkte proef naartoe sturen?</p>

            <div class="row-2">
              <div class="field">
                <label for="ts-name">Naam <span class="req">*</span></label>
                <input class="input" type="text" id="ts-name" name="name" autocomplete="name" required />
              </div>
              <div class="field">
                <label for="ts-brand">Merk- of winkelnaam <span class="req">*</span></label>
                <input class="input" type="text" id="ts-brand" name="brand" autocomplete="organization" required />
              </div>
            </div>

            <h4 style="margin:.6rem 0 1rem">Contact</h4>
            <div class="row-2">
              <div class="field">
                <label for="ts-email">E-mail <span class="req">*</span></label>
                <input class="input" type="email" id="ts-email" name="email" autocomplete="email" required />
              </div>
              <div class="field">
                <label for="ts-phone">Telefoon <span class="hint" style="font-weight:400">— optioneel</span></label>
                <input class="input" type="tel" id="ts-phone" name="phone" autocomplete="tel" placeholder="+31 6 &hellip;" />
                <span class="hint">Voeg het toe als je wilt dat we je op WhatsApp berichten — vermeld je landcode.</span>
              </div>
            </div>

            <p class="hint" style="margin:.4rem 0 1.4rem">Je proef wordt geleverd via een downloadlink naar je e-mail. Indien nodig nemen we contact op via WhatsApp of e-mail.</p>

            <div class="flex" style="justify-content:space-between;margin-top:.4rem">
              <button type="button" class="btn btn-ghost" data-wizard-back>
                <svg viewBox="0 0 24 24" class="i" style="transform:rotate(180deg)"><path d="M5 12h14M13 6l6 6-6 6" /></svg>
                Terug
              </button>
              <button type="submit" class="btn btn-primary">Proef aanvragen</button>
            </div>
          </div>
        </form>
      </div>
      <p class="hint" style="text-align:center;margin-top:1rem">Eén proef per bedrijf &middot; een kleine verificatie van &euro;0,99 om misbruik te voorkomen &middot; meer betaal je niet voor de proef.</p>
    </div>
  </section>

  <!-- CTA BAND -->
  <section class="section-tight">
    <div class="container">
      <div class="cta-band reveal pending">
        <h2 class="display" style="font-size:clamp(2.2rem,5vw,3.6rem)">Liever gewoon even overleggen?</h2>
        <p class="lead" style="margin:1.2rem auto 0;text-align:center">Stuur ons je product op WhatsApp en we helpen je een stijl kiezen.</p>
        <div class="flex" style="justify-content:center;margin-top:2rem">
          <a href="/nl/order" class="btn btn-ghost btn-lg">Plaats een volledige bestelling</a>
          <a href="https://wa.me/31625436130?text=Hi%20VISUAILS%2C%20ik%20wil%20graag%20een%20proefvisual%20proberen." class="btn btn-wa btn-lg" target="_blank" rel="noopener">
            <svg class="i" viewBox="0 0 24 24"><path d="M21 11.5a8.38 8.38 0 0 1-8.5 8.5 8.5 8.5 0 0 1-4-1L3 20l1.5-4.5A8.38 8.38 0 0 1 3.5 11 8.5 8.5 0 0 1 12 3a8.38 8.38 0 0 1 8.5 8.5z" /></svg>
            WhatsApp <span class="rec-badge">Snelst</span>
          </a>
        </div>
      </div>
    </div>
  </section>
  <!-- Wizard step navigation is handled globally in src/scripts/interactions.js
       (document-level delegation) so it survives ClientRouter navigations. -->
</Layout>

<style>
  /* Order-form controls — page-scoped, matching the source. */
  .narrow { max-width: 820px; }
  .hidden-step { display: none; }
  .field { display: flex; flex-direction: column; gap: 0.45rem; margin-bottom: 1.1rem; }
  .field label { font-size: 0.88rem; font-weight: 600; color: var(--ink); }
  .req { color: var(--accent-bright); }
  .hint { font-size: 0.8rem; color: var(--ink-3); }
  .input, .textarea { width: 100%; padding: 0.85rem 1rem; background: var(--surface); border: 1px solid var(--line-strong); border-radius: var(--r-sm); color: var(--ink); transition: border-color 0.22s var(--ease), box-shadow 0.22s var(--ease); font-size: 0.98rem; font-family: var(--font-body); }
  .input::placeholder, .textarea::placeholder { color: var(--ink-3); }
  .input:focus, .textarea:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-soft); }
  .textarea { min-height: 110px; resize: vertical; }
  .row-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 1.1rem; }
  @media (max-width: 620px) { .row-2 { grid-template-columns: 1fr; } }
  .choice-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 0.8rem; }
  .choice { position: relative; border-radius: var(--r-sm); overflow: hidden; cursor: pointer; border: 1px solid var(--line-strong); transition: transform 0.24s var(--ease), border-color 0.24s var(--ease); background: var(--surface); display: block; }
  .choice:hover { transform: translateY(-2px); border-color: var(--accent-line); }
  .choice input { position: absolute; opacity: 0; pointer-events: none; }
  .choice .choice-media { aspect-ratio: 1; }
  .choice .choice-media img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .choice .choice-media.mono { display: grid; place-items: center; background: radial-gradient(125% 90% at 50% 6%, #FFFFFF, #F1EBE3 58%, #E2D9CD 100%); }
  .choice .choice-label { padding: 0.6rem 0.7rem; font-size: 0.85rem; color: var(--ink); font-weight: 500; }
  .choice:has(input:checked) { border-color: var(--accent); box-shadow: 0 0 0 1px var(--accent); }
  .choice:has(input:checked)::after { content: "\2713"; position: absolute; top: 8px; right: 8px; width: 24px; height: 24px; border-radius: 50%; background: var(--accent); color: #fff; display: grid; place-items: center; font-size: 0.8rem; font-weight: 700; z-index: 5; }
  .choice .mono-av { position: relative; z-index: 2; display: grid; place-items: center; width: 100%; height: 100%; font-family: var(--font-display); font-weight: 900; font-size: 1.4rem; color: var(--accent-bright); }
  .choice .mono-av small { position: absolute; bottom: 8px; font-size: 0.58rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--ink-3); font-family: var(--font-body); font-style: normal; }
  .dropzone { display: block; border-radius: var(--r-sm); padding: 1.8rem; text-align: center; color: var(--ink-3); border: 1.5px dashed var(--line-strong); transition: border-color 0.22s var(--ease), background 0.22s var(--ease); cursor: pointer; background: var(--surface); }
  .dropzone:hover { border-color: var(--accent-line); background: var(--accent-soft); }
  .dropzone svg { width: 30px; height: 30px; margin: 0 auto 0.6rem; color: var(--accent-bright); }
  .progress { height: 3px; background: var(--surface-2); border-radius: var(--r-pill); overflow: hidden; margin-bottom: 1.6rem; }
  .progress > i { display: block; height: 100%; background: var(--accent); transition: width 0.5s var(--ease); }
  .chip-row { display: flex; flex-wrap: wrap; gap: 0.5rem; }
  .chip { display: inline-flex; align-items: center; gap: 0.45rem; padding: 0.4rem 0.9rem; border-radius: var(--r-pill); border: 1px solid var(--line); font-size: 0.8rem; color: var(--ink-2); }
  .chip .dot { width: 5px; height: 5px; border-radius: 50%; background: var(--accent); }
</style>
