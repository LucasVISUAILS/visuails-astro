---
// VISUAILS — order lifestyle visuals. Ported from the SvelteKit
// /order-lifestyle route: a 3-step order wizard (contact -> product/format/
// model -> quality/delivery) with a live running total. All steps live
// inside one <form> and stay mounted the whole time (display toggled via a
// class, not removed from the DOM) so field values survive stepping back
// and forth.
//
// The Svelte source used $state (step/qty/format/quality/delivery) and
// $derived (the running total) to drive this — real multi-step-wizard
// interactivity, load-bearing rather than cosmetic, so it's reproduced
// below with a page-scoped vanilla-JS block (classList toggles + a change
// listener that recomputes the total) rather than simplified away.
import Layout from '../../layouts/Layout.astro';

const roster = ['Aaron', 'Ava', 'Dana', 'Elias', 'Fabi', 'Lisa', 'Maegan', 'Rae', 'Ryan', 'Seme'];
---

<Layout
  title="Lifestylevisuals bestellen — VISUAILS"
  description="Bestel gestylde, realistische lifestylevisuals met optionele consistente modellen. Vanaf €35 per visual, afrekenen in drie snelle stappen."
>
  <section class="page-hero" style="padding-bottom:0">
    <div class="container">
      <p class="eyebrow-page"><a href="/nl/order" style="color:inherit">&larr; Alle bestelopties</a></p>
    </div>
  </section>

  <section id="lifestyle" class="ord-anchor section-tight">
    <div class="container narrow">
      <div class="section-head">
        <h1 style="font-size:var(--t-h2)">Lifestylevisuals bestellen</h1>
        <p>Gestylde, realistische scènes met optionele consistente modellen — afrekenen in drie snelle stappen.</p>
      </div>
      <div class="card" style="padding:clamp(1.4rem,3vw,2.4rem)">
        <div class="ls-photo">
          <img src="/img/lifestyle-glow-05.webp" alt="Voorbeeld lifestylevisual, Glow-stijl" loading="lazy" />
        </div>
        <div style="display:flex;flex-wrap:wrap;justify-content:space-between;align-items:baseline;gap:.6rem;margin-bottom:.9rem">
          <h3 style="margin:0">Plaats je bestelling</h3>
          <strong style="color:var(--accent-bright)">Vanaf &euro;35 / visual</strong>
        </div>
        <div class="chip-row" style="margin-bottom:1.4rem">
          <span class="chip"><span class="dot"></span>Veilig afrekenen</span>
          <span class="chip"><span class="dot"></span>Geleverd binnen ~24 uur</span>
          <span class="chip"><span class="dot"></span>Geen abonnement nodig</span>
        </div>

        <!-- TODO: wire this form to a real submit handler (Cloudflare Pages Function, Formspree, etc.) before launch — no backend in this static build yet -->
        <form action="/nl/thank-you" method="get" id="ls-form" novalidate>
          <div class="progress" aria-hidden="true"><i id="ls-progress-bar" style="width:33.33%"></i></div>

          <!-- STEP 1 · Contact -->
          <div class="step-panel" data-step="1">
            <h4 style="margin-bottom:1.2rem">Stap 1 &middot; Contact</h4>
            <div class="field">
              <label for="ls-name">Naam <span class="req">*</span></label>
              <input class="input" type="text" id="ls-name" name="name" autocomplete="name" required />
            </div>
            <div class="row-2">
              <div class="field">
                <label for="ls-company">Merk of winkelnaam <span class="req">*</span></label>
                <input class="input" type="text" id="ls-company" name="brand" autocomplete="organization" required />
              </div>
              <div class="field">
                <label for="ls-web">Website</label>
                <input class="input" type="url" id="ls-web" name="website" placeholder="https://" autocomplete="url" />
              </div>
            </div>
            <div class="row-2">
              <div class="field">
                <label for="ls-email">E-mail <span class="req">*</span></label>
                <input class="input" type="email" id="ls-email" name="email" autocomplete="email" required />
              </div>
              <div class="field">
                <label for="ls-phone">Telefoon <span class="hint" style="font-weight:400">— optioneel</span></label>
                <input class="input" type="tel" id="ls-phone" name="phone" autocomplete="tel" placeholder="+31 6 &hellip;" />
              </div>
            </div>
            <div class="field">
              <label for="ls-vat">Btw-nummer</label>
              <input class="input" type="text" id="ls-vat" name="vat" />
              <span class="hint">Optioneel — versnelt de facturering als je er een hebt.</span>
            </div>
            <div class="flex" style="justify-content:flex-end;margin-top:.4rem">
              <button type="button" class="btn btn-primary" data-step-next="2">Volgende
                <svg viewBox="0 0 24 24" class="i"><path d="M5 12h14M13 6l6 6-6 6" /></svg>
              </button>
            </div>
          </div>

          <!-- STEP 2 · Product, format & model -->
          <div class="step-panel hidden-step" data-step="2">
            <h4 style="margin-bottom:1.2rem">Stap 2 &middot; Product, formaat &amp; model</h4>

            <div class="field">
              <label for="ls-qty">Aantal verschillende producten <span class="req">*</span></label>
              <select class="select" id="ls-qty" name="quantity" required>
                <option value="1">1 product</option>
                <option value="2">2 producten</option>
                <option value="3">3 producten</option>
                <option value="4">4 producten</option>
                <option value="5">5 producten</option>
              </select>
              <span class="hint">De prijs schaalt per product. Add-ons hieronder gelden één keer per bestelling.</span>
            </div>

            <div class="field">
              <label for="ls-upload">Upload productfoto's <span class="req">*</span></label>
              <label class="dropzone" for="ls-upload">
                <svg viewBox="0 0 24 24" class="i"><path d="M12 16V4M6 10l6-6 6 6M4 20h16" /></svg>
                <div><strong>Klik om te uploaden</strong> of sleep bestanden hierheen</div>
                <div class="hint" style="margin-top:.4rem">Eén heldere foto per product is genoeg om te beginnen.</div>
                <input type="file" id="ls-upload" name="photos" accept="image/*" multiple hidden />
              </label>
            </div>

            <div class="field">
              <label>Kies formaatexport <span class="req">*</span></label>
              <label class="opt"><input type="radio" name="format" value="1:1" checked required /><span class="opt-main"><span class="opt-title">1:1 — Vierkant</span><span class="opt-desc">Marktplaatsen, productrasters, advertenties.</span></span><span class="opt-price opt-price-free">Inbegrepen</span></label>
              <label class="opt"><input type="radio" name="format" value="4:5" /><span class="opt-main"><span class="opt-title">4:5 — Portret</span><span class="opt-desc">Instagram-feed, productpagina's.</span></span><span class="opt-price opt-price-free">Inbegrepen</span></label>
              <label class="opt"><input type="radio" name="format" value="9:16" /><span class="opt-main"><span class="opt-title">9:16 — Verticaal</span><span class="opt-desc">Stories, Reels, TikTok.</span></span><span class="opt-price opt-price-free">Inbegrepen</span></label>
              <label class="opt"><input type="radio" name="format" value="16:9" /><span class="opt-main"><span class="opt-title">16:9 — Breed</span><span class="opt-desc">Banners, hero-secties.</span></span><span class="opt-price opt-price-free">Inbegrepen</span></label>
              <label class="opt"><input type="radio" name="format" value="Multi Format Export" /><span class="opt-main"><span class="opt-title">Multi-format export (alle 4)</span><span class="opt-desc">Elke verhouding hierboven, samen geleverd.</span></span><span class="opt-price">+&euro;19,99</span></label>
            </div>

            <div class="field">
              <label>Kies model <span class="req">*</span></label>
              <div class="choice-grid">
                {roster.map((m, i) => (
                  <label class="choice">
                    <input type="radio" name="model" value={m} required={i === 0} />
                    <div class="choice-media"><img src={`/img/model-${m.toLowerCase()}.webp`} alt={m} loading="lazy" /></div>
                    <div class="choice-label">{m}</div>
                  </label>
                ))}
                <label class="choice">
                  <input type="radio" name="model" value="VISUAILS choose" />
                  <div class="choice-media mono"><div class="mono-av"><svg viewBox="0 0 24 24" class="i" style="width:26px;height:26px;color:var(--accent-bright)"><path d="M12 2l2.4 7.4H22l-6 4.6 2.3 7.4-6.3-4.6L5.7 21.4 8 14 2 9.4h7.6z" /></svg></div></div>
                  <div class="choice-label">Laat VISUAILS kiezen</div>
                </label>
              </div>
            </div>

            <div class="card" style="background:var(--accent-soft);border-color:var(--accent-line)">
              <h4 style="margin-bottom:.7rem">Voor het beste resultaat</h4>
              <ul class="checklist">
                <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Een heldere frontafbeelding van elk product</span></li>
                <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Close-ups voor elk detail dat ertoe doet</span></li>
                <li><svg viewBox="0 0 24 24" class="i" style="stroke:var(--success)"><path d="M20 6L9 17l-5-5" /></svg><span>Goede belichting en een schone achtergrond</span></li>
              </ul>
            </div>

            <div class="flex" style="justify-content:space-between;margin-top:1.2rem">
              <button type="button" class="btn btn-ghost" data-step-prev="1">
                <svg viewBox="0 0 24 24" class="i" style="transform:rotate(180deg)"><path d="M5 12h14M13 6l6 6-6 6" /></svg>
                Terug
              </button>
              <button type="button" class="btn btn-primary" data-step-next="3">Volgende
                <svg viewBox="0 0 24 24" class="i"><path d="M5 12h14M13 6l6 6-6 6" /></svg>
              </button>
            </div>
          </div>

          <!-- STEP 3 · Quality & delivery -->
          <div class="step-panel hidden-step" data-step="3">
            <h4 style="margin-bottom:1.2rem">Stap 3 &middot; Kwaliteit &amp; levering</h4>

            <div class="field">
              <label>Exportkwaliteit <span class="req">*</span></label>
              <label class="opt"><input type="radio" name="quality" value="2K" checked required /><span class="opt-main"><span class="opt-title">2K — Standaard</span><span class="opt-desc">Scherp, klaar voor web en marktplaatsen.</span></span><span class="opt-price opt-price-free">Inbegrepen</span></label>
              <label class="opt"><input type="radio" name="quality" value="4K" /><span class="opt-main"><span class="opt-title">4K — Ultrahoge resolutie</span><span class="opt-desc">&asymp;&euro;2 per visual bij bestelling van 5.</span></span><span class="opt-price">+&euro;9,99</span></label>
            </div>

            <div class="field">
              <label>Levering <span class="req">*</span></label>
              <label class="opt"><input type="radio" name="delivery" value="Standard" checked required /><span class="opt-main"><span class="opt-title">Standaard</span><span class="opt-desc">Geleverd in de normale wachtrij.</span></span><span class="opt-price opt-price-free">Inbegrepen</span></label>
              <label class="opt"><input type="radio" name="delivery" value="Priority" /><span class="opt-main"><span class="opt-title">Priority — bovenaan de wachtrij</span><span class="opt-desc">We plaatsen je bestelling vooraan.</span></span><span class="opt-price">+&euro;29,99</span></label>
            </div>
            <p class="hint" style="margin-bottom:1.4rem">We streven naar levering binnen ~24 uur (kan variëren in drukke periodes).</p>

            <div class="flex" style="justify-content:space-between;margin-top:.4rem;align-items:center">
              <button type="button" class="btn btn-ghost" data-step-prev="2">
                <svg viewBox="0 0 24 24" class="i" style="transform:rotate(180deg)"><path d="M5 12h14M13 6l6 6-6 6" /></svg>
                Terug
              </button>
              <button type="submit" class="btn btn-primary btn-lg">Verstuur <span id="ls-total">&euro;35</span></button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </section>

  <section class="section-tight">
    <div class="container">
      <div class="section-head">
        <h2 style="font-size:1.4rem">Op zoek naar iets anders?</h2>
      </div>
      <div class="chip-row">
        <a href="/nl/order-catalog" class="chip">Catalogus</a>
        <a href="/nl/order-video" class="chip">Video</a>
        <a href="/nl/order-custom" class="chip">Custom styling</a>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Order-form controls — page-scoped (see order-catalog.astro for the same
     block; duplicated per-page rather than added to global.css). */
  .narrow { max-width: 820px; }
  .ls-photo { border-radius: var(--r-media); overflow: hidden; max-height: 200px; margin-bottom: 1.4rem; }
  .ls-photo img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .hidden-step { display: none; }
  .field { display: flex; flex-direction: column; gap: 0.45rem; margin-bottom: 1.1rem; }
  .field label { font-size: 0.88rem; font-weight: 600; color: var(--ink); }
  .req { color: var(--accent-bright); }
  .hint { font-size: 0.8rem; color: var(--ink-3); }
  .input, .select { width: 100%; padding: 0.85rem 1rem; background: var(--surface); border: 1px solid var(--line-strong); border-radius: var(--r-sm); color: var(--ink); transition: border-color 0.22s var(--ease), box-shadow 0.22s var(--ease); font-size: 0.98rem; font-family: var(--font-body); }
  .input:focus, .select:focus { outline: none; border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-soft); }
  .select option { background: var(--surface); color: var(--ink); }
  .row-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 1.1rem; }
  @media (max-width: 620px) { .row-2 { grid-template-columns: 1fr; } }
  .choice-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 0.8rem; }
  .choice { position: relative; border-radius: var(--r-sm); overflow: hidden; cursor: pointer; border: 1px solid var(--line-strong); transition: transform 0.24s var(--ease), border-color 0.24s var(--ease); background: var(--surface); display: block; }
  .choice:hover { transform: translateY(-2px); border-color: var(--accent-line); }
  .choice input { position: absolute; opacity: 0; pointer-events: none; }
  .choice .choice-media { position: relative; aspect-ratio: 1; background: var(--bg-deep); overflow: hidden; }
  .choice .choice-media img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .choice .choice-label { padding: 0.6rem 0.7rem; font-size: 0.85rem; color: var(--ink); font-weight: 500; }
  .choice:has(input:checked) { border-color: var(--accent); box-shadow: 0 0 0 1px var(--accent); }
  .choice:has(input:checked)::after { content: "\2713"; position: absolute; top: 8px; right: 8px; width: 24px; height: 24px; border-radius: 50%; background: var(--accent); color: #fff; display: grid; place-items: center; font-size: 0.8rem; font-weight: 700; }
  .mono { display: grid; place-items: center; background: radial-gradient(125% 90% at 50% 6%, #FFFFFF, #F1EBE3 58%, #E2D9CD 100%); }
  .mono .mono-av { position: relative; display: grid; place-items: center; width: 100%; height: 100%; font-family: var(--font-display); font-weight: 900; font-size: 1.4rem; color: var(--accent-bright); }
  .mono .mono-av small { position: absolute; bottom: 8px; font-size: 0.58rem; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase; color: var(--ink-3); font-family: var(--font-body); font-style: normal; }
  .opt { display: flex; align-items: center; gap: 0.8rem; padding: 0.9rem 1.1rem; border-radius: var(--r-sm); border: 1px solid var(--line-strong); cursor: pointer; transition: border-color 0.2s var(--ease), background 0.2s var(--ease); margin-bottom: 0.6rem; background: var(--surface); }
  .opt:has(input:checked) { border-color: var(--accent); background: var(--accent-soft); }
  .opt input { accent-color: var(--accent); width: 18px; height: 18px; flex: none; }
  .opt .opt-main { flex: 1; display: flex; flex-direction: column; gap: .15rem; }
  .opt .opt-title { color: var(--ink); font-weight: 500; }
  .opt .opt-desc { font-size: 0.82rem; color: var(--ink-3); }
  .opt .opt-price { color: var(--accent-bright); font-weight: 700; font-size: 0.9rem; }
  .opt .opt-price-free { color: var(--ink-3); font-weight: 600; font-size: 0.78rem; letter-spacing: .03em; text-transform: uppercase; }
  .dropzone { display: block; border-radius: var(--r-sm); padding: 1.8rem; text-align: center; color: var(--ink-3); border: 1.5px dashed var(--line-strong); transition: border-color 0.22s var(--ease), background 0.22s var(--ease); cursor: pointer; background: var(--surface); }
  .dropzone:hover { border-color: var(--accent-line); background: var(--accent-soft); }
  .dropzone svg { width: 30px; height: 30px; margin: 0 auto 0.6rem; color: var(--accent-bright); }
  .progress { height: 3px; background: var(--surface-2); border-radius: var(--r-pill); overflow: hidden; margin-bottom: 1.6rem; }
  .progress > i { display: block; height: 100%; background: var(--accent); transition: width 0.5s var(--ease); }
  .chip-row { display: flex; flex-wrap: wrap; gap: 0.5rem; }
  .chip { display: inline-flex; align-items: center; gap: 0.45rem; padding: 0.4rem 0.9rem; border-radius: var(--r-pill); border: 1px solid var(--line); font-size: 0.8rem; color: var(--ink-2); }
  .chip .dot { width: 5px; height: 5px; border-radius: 50%; background: var(--accent); }
  a.chip { transition: border-color var(--dur) var(--ease), color var(--dur) var(--ease); }
  a.chip:hover { border-color: var(--accent-line); color: var(--ink); }
  .ord-anchor { scroll-margin-top: 100px; }
</style>

<!-- Wizard step navigation + running total handled globally in
     src/scripts/interactions.js (delegation) so it survives ClientRouter navs. -->
