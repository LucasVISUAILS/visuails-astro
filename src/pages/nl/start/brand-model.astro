---
// VISUAILS — /nl/start/brand-model. A placeholder that says so.
// The page body is HoldingPage.astro, shared with the other two services that
// have no checkout yet, so the three read as one deliberate category.
import Layout from '../../../layouts/Layout.astro';
import HoldingPage from '../../../components/order/HoldingPage.astro';
---

<Layout title="Een merkmodel aanvragen — VISUAILS" description="Eén gezicht, alleen voor jou, eenmalig. Er zit geen afrekenknop op: het wordt gebouwd vanuit een briefing, dus het begint met een gesprek.">
  <HoldingPage lang="nl" kind="brand-model" />
</Layout>
