---
// VISUAILS — /nl/start/video. A placeholder that says so.
// The page body is HoldingPage.astro, shared with the other two services that
// have no checkout yet, so the three read as one deliberate category.
import Layout from '../../../layouts/Layout.astro';
import HoldingPage from '../../../components/order/HoldingPage.astro';
---

<Layout title="Videoclips — VISUAILS" description="De bestelflow voor video is niet af. Clips worden nu met de hand ingepland — zeg wat je nodig hebt en de studio antwoordt schriftelijk.">
  <HoldingPage lang="nl" kind="video" />
</Layout>
