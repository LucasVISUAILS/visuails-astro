---
// VISUAILS — /studio (English). Body in StudioPage.astro so EN and NL cannot
// drift; only the meta differs here.
import Layout from '../layouts/Layout.astro';
import StudioPage from '../components/StudioPage.astro';
---

<Layout
  title="How an order is run — the calendar | VISUAILS"
  description="A date is only worth the mechanism behind it. The calendar counts real load in products, refuses a week it cannot hold, and offers the first one it can."
  ogImage="/img/banners-13.webp"
  ground="espresso"
>
  <StudioPage lang="en" />
</Layout>
