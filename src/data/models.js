// VISUAILS — the standard model roster.
//
// WHY THIS FILE EXISTS
// Task #268 restored /models as a real page after Lucas asked for the
// standard-models entry point back (see AUDIT-TASK-0.md §H·8 for the
// original merge and its reasoning, and the note appended there on
// 2026-07-29 for why it un-merged). Before this file existed, the ten-model
// roster was typed once, inline, inside BrandModelPage.astro. Splitting the
// roster across two pages without a shared source would have meant editing
// the same ten names and photo paths in two files every time one changes —
// exactly the kind of drift src/data/pricing.js exists to prevent for euro
// figures. This is that same discipline for the model roster.
//
// Names and traits are ours, not clients' — the "never invent client names"
// rule is about attribution, and nothing here is attributed to anyone. The
// photos are our own output.
// `w`/`h` are the file's INTRINSIC pixel size, and they are here rather than
// typed at each call site because they were being typed at each call site and
// getting it wrong: /models declared 600x750 for a 1195x1600 file, the
// homepage rail declared 800x1071 for all ten when only Rae is that size.
// Nothing looked squashed — every one of them is object-fit: cover — but the
// box the browser reserved before the bytes landed had the wrong aspect ratio,
// which is layout shift on exactly the images a visitor is looking at.
// Rae is genuinely a different size from the other nine. That is the reason
// this cannot be one shared constant.
//
// `thumb`/`tw`/`th` are the SAME PHOTOGRAPH at roster-grid size, and they are
// here for the same reason `w`/`h` are. No grid on this site draws a roster
// portrait larger than 390 CSS px — /models is 211px on a desktop and 350px on
// a phone — so `photo`, at 1195px, was ~1.3 MB of pixels downloaded to be
// thrown away on one page load. The derivatives are 800px wide: 2x the widest
// box the portraits are ever drawn in, measured in a browser. They are
// generated by scripts/make-thumbs.mjs, which is also where the width and the
// encode quality are argued.
//
// USE `thumb` WHEREVER THE PORTRAIT IS A GRID TILE, AND `photo` WHEN IT IS
// NOT. `photo` stays because 800px is a thumbnail, not a replacement: anything
// that ever shows a portrait large — a model's own page, a hero split — must
// keep reading `photo`. Pairing each path with its own size, rather than one
// `w`/`h` for both, is what stops a call site pointing at the 800px file while
// declaring 1195x1600.
//
// Rae's `thumb` is her original: it is already 800x1071, exactly the target,
// so a derivative would be a same-size re-encode. That every other portrait
// resizes to 800x1071 too is arithmetic, not coincidence — 1195x1600 and
// 800x1071 are the same 3:4 to within a rounding pixel.
export const ROSTER = [
  { name: 'Aaron',  photo: '/img/model-aaron.webp',  thumb: '/img/model-aaron-w800.webp',  traits: ['warm', 'approachable'], w: 1195, h: 1600, tw: 800, th: 1071 },
  { name: 'Ava',    photo: '/img/model-ava.webp',    thumb: '/img/model-ava-w800.webp',    traits: ['editorial', 'elegant'], w: 1195, h: 1600, tw: 800, th: 1071 },
  { name: 'Elias',  photo: '/img/model-elias.webp',  thumb: '/img/model-elias-w800.webp',  traits: ['refined', 'classic'], w: 1195, h: 1600, tw: 800, th: 1071 },
  { name: 'Ryan',   photo: '/img/model-ryan.webp',   thumb: '/img/model-ryan-w800.webp',   traits: ['sporty', 'energetic'], w: 1195, h: 1600, tw: 800, th: 1071 },
  { name: 'Dana',   photo: '/img/model-dana.webp',   thumb: '/img/model-dana-w800.webp',   traits: ['confident', 'modern'], w: 1195, h: 1600, tw: 800, th: 1071 },
  { name: 'Lisa',   photo: '/img/model-lisa.webp',   thumb: '/img/model-lisa-w800.webp',   traits: ['natural', 'approachable'], w: 1195, h: 1600, tw: 800, th: 1071 },
  { name: 'Maegan', photo: '/img/model-maegan.webp', thumb: '/img/model-maegan-w800.webp', traits: ['bold', 'statement'], w: 1195, h: 1600, tw: 800, th: 1071 },
  { name: 'Rae',    photo: '/img/model-rae.webp',    thumb: '/img/model-rae.webp',         traits: ['soft', 'understated'], w: 800, h: 1071, tw: 800, th: 1071 },
  { name: 'Fabi',   photo: '/img/model-fabi.webp',   thumb: '/img/model-fabi-w800.webp',   traits: ['clean', 'contemporary'], w: 1195, h: 1600, tw: 800, th: 1071 },
  { name: 'Seme',   photo: '/img/model-seme.webp',   thumb: '/img/model-seme-w800.webp',   traits: ['sharp', 'editorial'], w: 1195, h: 1600, tw: 800, th: 1071 },
];

/**
 * The value a roster entry travels as — in the order form, in details_json,
 * and in whatever the studio reads off it.
 *
 * Derived rather than typed as an eleventh column, because a second field
 * holding the same fact is a second field that can disagree with the first.
 * Every name in ROSTER is ASCII and single-word, so lowercasing is total and
 * reversible; if a name ever arrives with a space or an accent, this is the
 * one place that has to learn about it.
 */
export const modelId = (name) => String(name).toLowerCase();

/** The value meaning "no preference" — a real answer, not an empty field. */
export const MODEL_ANY = 'any';

// Trait keys -> label, per language. Kept here rather than duplicated in
// every page's own COPY object for the same reason ROSTER is centralized:
// two pages now render every trait key in ROSTER above, and a trait added to
// one roster entry without a matching label here would render blank rather
// than fail loudly, so keeping the two arrays beside each other is the
// point.
export const TRAITS = {
  en: {
    warm: 'Warm', approachable: 'Approachable', editorial: 'Editorial', elegant: 'Elegant',
    refined: 'Refined', classic: 'Classic', sporty: 'Sporty', energetic: 'Energetic',
    confident: 'Confident', modern: 'Modern', natural: 'Natural', bold: 'Bold',
    statement: 'Statement', soft: 'Soft', understated: 'Understated', clean: 'Clean',
    contemporary: 'Contemporary', sharp: 'Sharp',
  },
  nl: {
    warm: 'Warm', approachable: 'Toegankelijk', editorial: 'Editorial', elegant: 'Elegant',
    refined: 'Verfijnd', classic: 'Klassiek', sporty: 'Sportief', energetic: 'Energiek',
    confident: 'Zelfverzekerd', modern: 'Modern', natural: 'Natuurlijk', bold: 'Gedurfd',
    statement: 'Statement', soft: 'Zacht', understated: 'Ingetogen', clean: 'Clean',
    contemporary: 'Eigentijds', sharp: 'Scherp',
  },
};
