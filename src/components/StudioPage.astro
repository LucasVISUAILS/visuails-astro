---
// VISUAILS — /studio, the public page for the studio side. Section 17.
//
// THE ARGUMENT THIS PAGE MAKES. Everywhere else on the site the studio says
// "a reserved window, confirmed before you pay". That is a promise, and a
// promise is only worth the mechanism behind it. This page is the mechanism:
// the capacity calendar, what it refuses, and why a date offered here is one
// the studio has already checked it can keep. It is the page a sceptical buyer
// reads when they have heard "48 hours" from someone before.
//
// WHAT IT DOES NOT CLAIM. Flag 15i: no notification of any kind fires on a
// status change — no WhatsApp, no email. So nothing here says you will be
// told. What is true, and what the copy points at instead, is that the change
// writes the client-visible timeline in the same move, so the portal is never
// behind the studio. If a notifier is built later, this page gains a sentence;
// it does not have to lose one.
//
// WHAT THE AUGUST 2026 PRICING MODEL CHANGED HERE, and what it did not. This
// page prints no price and it still prints none — the mechanism is the subject,
// so there is nothing on it to re-quote. One paragraph was load-bearing and had
// to move: the capacity rule used to read "drops outrank loose products", which
// described a PRIORITY BETWEEN TWO PRODUCTS the buyer chose between. Under the
// ladder (section 0 of src/data/pricing.js) there is no such choice — service
// level follows order size, and WINDOW_THRESHOLD is the whole of the rule. So
// the rule is stated as the threshold now, read from pricing.js rather than
// described in prose, which is also what stops this page and /pricing giving
// two different accounts of the same gate.
//
// The word "drop" is gone from the copy for the reason pricing.js gives: to the
// brands we sell to it means a collection going live, and using it for a work
// order made one word carry two meanings. What the studio runs is an order —
// een bestelling.
//
// ── DE TWEE GRIJZE VLAKKEN ZIJN WEG, 9 AUGUSTUS 2026 ────────────────────────
//
// Lucas: *"maak de mock ups voor deze 'Our side' pagina en houd het consistent."*
//
// Er stonden twee `.ph`-placeholders op deze pagina — "ADMINPORTAAL · DE AGENDA"
// en "ADMINPORTAAL · BESTELLINGENTABEL" — met eronder een noot die beschreef wat
// er te zien had moeten zijn. Op de pagina waarvan de hele functie is te bewijzen
// dat een leverdatum een mechanisme heeft. Beide noten zijn nu de opdracht van
// een echte figuur geworden: FigGate en FigBoard.
//
// EN ÉÉN DING KLOPTE NIET, wat pas opviel toen de figuur uit de echte cijfers
// getekend werd. Deze pagina zei "a week has room for a number of products",
// terwijl src/data/capacity.js per DAG telt: 18 producten per studiodag, 3 daarvan
// vrij voor kleine bestellingen, en een venster van twee open dagen. Weken bestaan
// nergens in dat bestand. De regel is daarom aangepast naar de eenheid die de
// poort werkelijk gebruikt — anders zou de figuur ernaast de tekst tegenspreken,
// en dan is een mock-up erger dan een grijs vlak.
import { WINDOW_THRESHOLD, aim48 } from '../data/pricing.js';
import { PRODUCTS_PER_DAY, QUEUE_FLOOR_PER_DAY, WINDOW_DAYS } from '../data/capacity.js';
import { localizedPath } from '../i18n/ui.js';
import FigGate from './FigGate.astro';
import FigBoard from './FigBoard.astro';
import Placeholder from './Placeholder.astro';
import '../styles/stijl22.css';

interface Props { lang: 'en' | 'nl'; }
const { lang } = Astro.props;
const lp = (p: string) => localizedPath(lang, p);

const COPY = {
  en: {
    h1: ['How we arrive at a delivery date.', 'This is the calendar we work in.'],
    lede: 'Anyone can put "48 hours" on a website. The question is what happens in the week it is not true. Here is the part of the studio that answers that, and the reason a date is never offered before it has been checked — and never as a promise we have not reserved the day for.',
    ctaStart: 'Start an order',
    ctaPortal: 'See a delivered order',

    gateH: 'We check the calendar before we offer you a date.',
    gateLede: 'The date picker on the order form does not show a list of dates. It asks the calendar what the studio has actually committed to on those days, removes every window the studio cannot hold, and offers the first one it can.',
    gate: [
      // Zie de noot bovenaan dit bestand: dit zei "a week has room", en de poort
      // telt per dag. De cijfers komen nu uit capacity.js, dus de tekst kan niet
      // meer los gaan staan van het mechanisme dat hij beschrijft.
      ['Counted in products, not orders', `A studio day holds ${PRODUCTS_PER_DAY} products, and a reserved window is ${WINDOW_DAYS} days. One order of thirty products asks for fifteen on each of those two days \u2014 not "thirty somewhere in there".`],
      ['One ceiling, one place', `The limit is a single configurable number, and ${QUEUE_FLOOR_PER_DAY} of every ${PRODUCTS_PER_DAY} stay off it so a small order still runs in a normal turnaround. There is no second rule anywhere that can quietly disagree.`],
      // Size, not product type. See the header note: this used to say "drops
      // outrank loose products", which named two things a buyer picked between.
      [`Size decides, at ${WINDOW_THRESHOLD} products`, `From ${WINDOW_THRESHOLD} products an order goes into the calendar and holds a committed date. Smaller orders run in a normal turnaround and yield to it, which is what makes the committed date committed.`],
      ['Nothing is promised twice', 'A window that has been offered and taken is gone from the picker before the next person sees it.'],
    ],

    boardH: 'One table, every order.',
    boardLede: 'Our side of it is deliberately boring: a list of orders, the state each one is in, and the calendar the picker reads from. There is no dashboard of graphs, because a graph has never told anyone which order is late.',
    board: [
      ['Every order, one row', 'Reference, brand, product count, the window it holds and the state it is in.'],
      ['Revisions come to the top', 'A revision request from a client portal lands in its own list, with the note the client wrote, in their words.'],
      ['A state change writes both sides', 'Moving an order writes the studio record and the client-visible timeline in the same move — which is why the portal is never out of date with the studio.'],
    ],

    humanH: 'The last step is a person.',
    humanLede: 'Nothing is auto-delivered. Every visual in every order is looked at before it goes out, and the looking is a state the order has to pass through rather than a claim on a page.',
    humanNote: 'The log records that review. Every approval and every revision is saved with a date against the image it belongs to. The same record you use to approve the order is the one we can read back to you months later.',
    humanCta: 'Read the AI Act page',

    // ── TERUGREKENEN VANAF DE LANCEERDATUM ──────────────────────────────
    // Verhuisd van de homepage (sectie 14), augustus 2026. Dit is de enige
    // eerlijke schaarste-band die hier te maken valt: de krapte is echt — één
    // persoon en een capaciteitspoort die weken wéigert — en hij staat er als
    // capaciteit en niet als aftelklok. Geen "nog 3 plekken": dat is een getal
    // dat de pagina niet kan natrekken tegen de D1-kalender, en een
    // schaarsteclaim die een bezoeker kan ontkrachten is erger dan geen.
    plH: 'Work back from your launch date.',
    plLede: `From ${WINDOW_THRESHOLD} products we take that week out of the calendar for your order. That is why we can put the date in writing, and why a week that is full is offered as full.`,
    pl: [
      ['Your launch date', 'The day the collection has to be live. Everything else is counted backwards from here.'],
      ['Your reserved date', `The delivery date we hold, on orders of ${WINDOW_THRESHOLD} products or more. Once the calendar has cleared it, nothing in the studio may push it. ${aim48('en')} That part is what we work towards, not what we promise. Below that count an order runs in the normal turnaround.`],
      ['Minus one folder', 'The day you send the photos. That is your part finished, and the date is already locked.'],
    ],
    plNote: 'Weeks are booked one at a time, and a full week is offered as full rather than as a longer wait.',
    plCta: 'See which weeks are open',

    honestH: 'What this does not do.',
    honestLede: 'Worth saying plainly, because a page about a mechanism that only lists what works is a brochure.',
    honest: [
      ['It does not guarantee a window it has not cleared', 'If the calendar is full, you are offered the next window that is not. You will not be told yes and then rescheduled.'],
      ['It does not rush an order past the review', 'The reserved delivery date is the date the work fits in, not a target the studio sprints at. An order that is not right does not ship on time; it ships right.'],
      ['It does not scale by hiring', 'The ceiling is a real number and it stays a real number. When the studio is full it says so, which is the only version of this that is worth anything.'],
    ],

    closeH: 'Book a window the calendar has cleared.',
    closeLede: 'The date you are offered is one the studio has already checked it can keep.',
    closeCta: 'Start an order',
    closeAlt: 'See it on your own product first',
  },

  nl: {
    h1: ['Zo komen we aan een leverdatum.', 'Dit is de agenda waar wij in werken.'],
    lede: 'Iedereen kan "48 uur" op een website zetten. De vraag is wat er gebeurt in de week dat het niet waar is. Dit is het deel van de studio dat die vraag beantwoordt, en de reden dat een datum nooit wordt aangeboden voordat hij gecontroleerd is — en nooit als belofte waar we de dag niet voor hebben vrijgehouden.',
    ctaStart: 'Start een bestelling',
    ctaPortal: 'Bekijk een geleverde bestelling',

    gateH: 'We kijken eerst in de agenda, en pas daarna noemen we een datum.',
    gateLede: 'De datumkiezer in het bestelformulier toont geen lijstje datums. Hij vraagt de planning waar de studio zich op die dagen werkelijk aan verbonden heeft, haalt elk venster weg dat de studio niet kan vasthouden, en biedt het eerstvolgende venster aan dat wél vrij te houden is.',
    gate: [
      ['Werkdruk, geen beschikbaarheid', `Een studiodag houdt ${PRODUCTS_PER_DAY} producten, en een vastgelegd venster is ${WINDOW_DAYS} dagen. Een bestelling van dertig vraagt vijftien op elk van die twee dagen — niet "dertig ergens daarin".`],
      ['Eén plafond, één plek', `De limiet is één instelbaar getal, en ${QUEUE_FLOOR_PER_DAY} van elke ${PRODUCTS_PER_DAY} blijven eraf zodat een kleine bestelling nog steeds in een normale doorlooptijd draait. Er is nergens een tweede regel die er stilletjes van kan afwijken.`],
      // Zie de EN-tak: omvang beslist, niet het soort bestelling.
      [`Omvang beslist, bij ${WINDOW_THRESHOLD} producten`, `Vanaf ${WINDOW_THRESHOLD} producten gaat een bestelling de agenda in en houdt hij een vastgelegde datum. Kleinere bestellingen draaien in een normale doorlooptijd en wijken ervoor — precies dat maakt de vastgelegde datum vastgelegd.`],
      ['Niets wordt twee keer beloofd', 'Een venster dat is aangeboden en gepakt, is uit de kiezer verdwenen voordat de volgende het ziet.'],
    ],

    boardH: 'Eén tabel, elke order.',
    boardLede: 'Onze kant is met opzet saai: een lijst orders, de staat waarin elke order zit, en de kalender waar de kiezer uit leest. Geen dashboard vol grafieken, want een grafiek heeft nog nooit iemand verteld welke bestelling te laat is.',
    board: [
      ['Elke order, één regel', 'Kenmerk, merk, aantal producten, het venster dat hij vasthoudt en de staat waarin hij zit.'],
      ['Revisies komen bovenaan', 'Een revisieverzoek uit VISUAILS Studio komt in een eigen lijst binnen, met de notitie die de klant schreef, in diens eigen woorden.'],
      ['Een statuswissel schrijft beide kanten', 'Een order verplaatsen schrijft het studiodossier en de tijdlijn die de klant ziet in dezelfde beweging — daarom loopt het portaal nooit achter op de studio.'],
    ],

    humanH: 'De laatste stap is een specialist.',
    humanLede: 'Er wordt niets automatisch geleverd. Elk beeld in elke bestelling wordt bekeken voordat het weggaat, en dat kijken is een staat waar de order doorheen moet — geen bewering op een pagina.',
    humanNote: 'Die controle is ook wat het logboek vastlegt. Elke goedkeuring en revisie krijgt een tijdstempel en wordt bewaard bij het beeld waar hij bij hoort, dus wat jou de regie geeft over de bestelling is hetzelfde wat de reeks later terug te lezen maakt.',
    humanCta: 'Lees de AI Act-pagina',

    plH: 'Reken terug vanaf de dag dat je live gaat.',
    plLede: 'Bij een vastgezette leverdatum houden we die dagen in de studio voor je vrij. Daarom kunnen we die datum op papier zetten — en daarom kan een week ook vol zitten.',
    pl: [
      ['De dag dat je live gaat', 'De dag waarop de collectie online moet staan. Vanaf daar rekenen we terug.'],
      ['Je vastgezette datum', `De leverdatum die we vrijhouden, vanaf ${WINDOW_THRESHOLD} producten. Zodra die datum vaststaat, gaat geen ander werk in de studio er nog voor. ${aim48('nl')} Dat is waar we naartoe werken, niet wat we beloven. Bestel je minder producten, dan geldt de normale doorlooptijd.`],
      ['Min één map', 'De dag dat jij de foto’s stuurt. Daarmee is jouw deel klaar, en de datum staat al vast.'],
    ],
    plNote: 'We boeken de weken één voor één vol. Zit een week vol, dan zeggen we dat gewoon, in plaats van je een langere wachttijd te geven.',
    plCta: 'Kijk welke weken nog vrij zijn',

    honestH: 'Wat dit niet doet.',
    honestLede: 'Dat is het waard om ronduit te zeggen, want een pagina over een werkwijze die alleen opsomt wat er goed gaat, is een folder.',
    honest: [
      ['Het garandeert geen venster dat niet is vrijgegeven', 'Zit de planning vol, dan krijg je het eerstvolgende venster dat nog vrij is. Je krijgt geen ja om daarna verzet te worden.'],
      ['Het jaagt een bestelling niet langs de controle', 'De vastgezette leverdatum is de leverdatum waarin het werk past, geen doel waar de studio naartoe sprint. Een bestelling die niet klopt gaat niet op tijd weg; die gaat goed weg.'],
      ['We rekenen niet op ruimte die er nog niet is', 'Het plafond is een echt getal en blijft een echt getal. Zit de studio vol, dan zeggen we dat gewoon — anders is die datum niets waard.'],
    ],

    closeH: 'Boek een venster dat de planning heeft vrijgegeven.',
    closeLede: 'De datum die je krijgt aangeboden, is er een waarvan de studio al heeft gecontroleerd dat hij hem kan houden.',
    closeCta: 'Start een bestelling',
    closeAlt: 'Probeer VISUAILS eerst voor €1',
  },
};

const c = COPY[lang];
/* Voor de briefteksten op de placeholders hieronder — die staan niet in COPY
   omdat ze niet aan de bezoeker gericht zijn maar aan degene die de foto maakt. */
const nl = lang === 'nl';
---

{/* ═══ /studio IN DE STIJL VAN SECTIE 22 — 7 september 2026 ═════════════════
    Dertiende pagina in de stijl. De inhoud is onaangeraakt: dezelfde COPY in
    beide talen, dezelfde <FigGate> en <FigBoard>, dezelfde twee <Placeholder>s
    met hun opdracht erin, dezelfde getallen uit capacity.js.

    DE POORT EN DE HANDCONTROLE ZIJN DE TWEE DONKERE KAMERS. Dat zijn ook de
    twee plekken waar de pagina van "hier is een scherm" naar "en dit is waarom
    het klopt" gaat, en een hoofdstukwissel is precies wat daar hoort.

    DE HERO DOET NIET MEE AAN DE INKOMEFFECTEN, en dat blijft zo: wat boven de
    vouw staat hoort er meteen te staan; een titel die eerst moet inkomen is een
    titel die je even niet kunt lezen. In deze stijl is dat geen keuze meer maar
    het gedrag van `.paneel` — er zit geen reveal op. */}
<div class="s22">

  {/* ── 1 · DE OPENING ───────────────────────────────────────────────────── */}
  <section class="paneel" aria-labelledby="sp-h1">
    <div class="sp-open">
      <div class="paneel-kop">
        <h1 id="sp-h1" class="hoog">{c.h1[0]} <em>{c.h1[1]}</em></h1>
        <p class="lopend">{c.lede}</p>
        <p class="acties">
          <a class="knop knop-inkt" href={lp('/start')}>{c.ctaStart}</a>
          <a class="knop knop-lijn" href={lp('/portal')}>{c.ctaPortal}</a>
        </p>
      </div>
      <Placeholder
        lang={lang}
        ratio="4 / 3"
        caption={false}
        subject={nl ? 'Schermafdruk van VISUAILS Studio — het overzicht met één lopende bestelling'
                    : 'Screenshot of VISUAILS Studio — the overview with one live order'}
        message={nl
          ? 'Moet in één blik laten zien dat dit een échte werkomgeving is en geen mailtje met een downloadlink: statusregel, leverdatum, en beelden die op goedkeuring wachten. Liever een halfvolle bestelling dan een lege — een leeg scherm bewijst niets.'
          : 'Must show at a glance that this is a real workspace and not an email with a download link: status row, delivery date, and images waiting for approval. A half-full order beats an empty one — an empty screen proves nothing.'}
      />
    </div>
  </section>

  {/* ── 2 · DE CAPACITEITSPOORT ─────────────────────────────────────────────
      De agenda zelf, met de dag erin die geweigerd wordt. Het venster dat hij
      "aangeboden" noemt komt uit offerableWindows() — zie FigGate.astro. */}
  <section class="kamer sp-kamer" aria-labelledby="sp-poort">
    <p class="spook spook-links" aria-hidden="true"><span>Studio</span><span>Studio</span></p>
    <header class="kamer-kop">
      <h2 id="sp-poort" class="hoog hoog-2">{c.gateH}</h2>
      <p class="lopend">{c.gateLede}</p>
    </header>
    <div class="sp-twee">
      <ul class="sp-regels" role="list">
        {c.gate.map(([k, v]) => (
          <li><span class="mono">{k}</span><b>{v}</b></li>
        ))}
      </ul>
      <FigGate lang={lang} />
    </div>
  </section>

  {/* ── 3 · HET BESTELBORD ──────────────────────────────────────────────────
      De tabel, met het revisieverzoek open. */}
  <section class="paneel" aria-labelledby="sp-bord">
    <div class="sp-twee-licht">
      <div class="sp-bord-t">
        <h2 id="sp-bord" class="hoog hoog-2">{c.boardH}</h2>
        <p class="lopend">{c.boardLede}</p>
        <dl class="sp-def">
          {c.board.map(([k, v]) => (<Fragment><dt>{k}</dt><dd class="lopend">{v}</dd></Fragment>))}
        </dl>
      </div>
      <FigBoard lang={lang} />
    </div>
  </section>

  {/* ── 4 · DE STAP MET DE HAND ─────────────────────────────────────────────
      "De laatste stap is een specialist" is de belangrijkste belofte op deze
      pagina en stond er ooit als drie alinea's tekst. Een foto van iemand die
      daadwerkelijk zit te kijken doet dat werk in een halve seconde — vandaar
      de plaatshouder met de opdracht erin, en niet een lichtstudie: een
      lichtkegel bewijst niet dat er met de hand gekeken wordt. */}
  <section class="kamer sp-kamer-hand" aria-labelledby="sp-hand">
    <p class="spook spook-rechts" aria-hidden="true"><span>Studio</span><span>Studio</span></p>
    <header class="kamer-kop">
      <h2 id="sp-hand" class="hoog hoog-2">{c.humanH}</h2>
      <p class="lopend">{c.humanLede}</p>
      <p class="lopend sp-noot">{c.humanNote}</p>
      <p class="acties"><a class="knop sp-knop-lijn" href={lp('/ai-act')}>{c.humanCta}</a></p>
    </header>
    <Placeholder
      lang={lang}
      ratio="16 / 9"
      caption={false}
      subject={nl ? 'Iemand aan het werk: een beeld op het scherm, ernaast de originele telefoonfoto van de klant'
                  : 'Someone at work: a finished visual on screen, the client’s original phone photo beside it'}
      message={nl
        ? 'Moet bewijzen dat "met de hand gecontroleerd" een handeling is en geen zinnetje. Het vergelijken moet te zien zijn — twee beelden naast elkaar, een blik erop. Geen poserend portret en geen stockfoto van een kantoor.'
        : 'Must prove that "checked by hand" is an action, not a phrase. The comparing has to be visible — two images side by side, a person looking at them. Not a posed portrait, not a stock office shot.'}
    />
  </section>

  {/* ── 5 · TERUGREKENEN VANAF DE LANCEERDATUM ──────────────────────────────
      Het leest als een dienst aan de lezer en niet als druk: een lanceerdatum
      is de ene datum die hij al in zijn hoofd heeft, en deze rij doet de
      aftrekking voor hem. Geen kleurladder over de drie stappen — zie de noot
      daarover in de voorpagina: drie stappen die van accent naar 40% wit liepen
      terwijl de derde de leverdatum is. */}
  <section class="paneel" aria-labelledby="sp-plan">
    <header class="paneel-kop">
      <h2 id="sp-plan" class="hoog hoog-2">{c.plH}</h2>
      <p class="lopend">{c.plLede}</p>
    </header>
    <ol class="sp-plan">
      {c.pl.map(([t, b], i) => (
        <li>
          <span class="mono nr">{String(i + 1).padStart(2, '0')}</span>
          <b>{t}</b>
          <span class="lopend">{b}</span>
        </li>
      ))}
    </ol>
    <div class="sp-plan-voet">
      <p class="lopend">{c.plNote}</p>
      <p class="acties"><a class="knop knop-lijn" href={lp('/start')}>{c.plCta}</a></p>
    </div>
  </section>

  {/* ── 6 · WAT HET NIET DOET ────────────────────────────────────────────── */}
  <section class="paneel" aria-labelledby="sp-eerlijk">
    <header class="paneel-kop">
      <h2 id="sp-eerlijk" class="hoog hoog-2">{c.honestH}</h2>
      <p class="lopend">{c.honestLede}</p>
    </header>
    <ul class="sp-eerlijk" role="list">
      {c.honest.map(([k, v]) => (
        <li><b>{k}</b><span class="lopend">{v}</span></li>
      ))}
    </ul>
  </section>

  {/* ── 7 · HET SLOT ─────────────────────────────────────────────────────── */}
  <section class="paneel paneel-slot" aria-labelledby="sp-slot">
    <h2 id="sp-slot" class="hoog">{c.closeH}</h2>
    <p class="lopend">{c.closeLede}</p>
    <p class="acties">
      <a class="knop knop-inkt" href={lp('/start')}>{c.closeCta}</a>
      <a class="knop knop-lijn" href={lp('/test-sample')}>{c.closeAlt}</a>
    </p>
  </section>
</div>

<style>
  .sp-open { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); gap: clamp(1.5rem, 5vw, 4rem); align-items: center; }
  .sp-open .paneel-kop { margin-bottom: 0; gap: 1.1rem; }

  .sp-twee { display: grid; grid-template-columns: minmax(0, .8fr) minmax(0, 1fr); gap: clamp(1.5rem, 4vw, 3.5rem); align-items: center; }
  /* De drie regels van de poort: het etiket klein, de uitleg als lopende tekst.
     Geen tegels met een rand eromheen — het zijn drie feiten over één ding.

     EN DE UITLEG IS GEEN GETAL. Hij stond hier eerst in de kopletter op
     clamp(1.3rem, 2.6vw, 2rem), want "etiket klein, cijfer groot" is de vorm
     die deze stijl elders gebruikt. Maar `c.gate` draagt hier geen cijfers: de
     waarde is een hele alinea ("A studio day holds 18 products, and a reserved
     window is 2 days…"). Drie alinea's op 32px in de kopletter is geen nadruk
     maar schreeuwen, en het duwt de tekening ernaast uit beeld. */
  .sp-regels { display: grid; gap: 1.4rem; }
  .sp-regels li { display: grid; gap: .4rem; padding-top: 1rem; border-top: 1px solid rgb(245 245 245 / .22); }
  .sp-regels .mono { color: rgb(245 245 245 / .66); }
  .sp-regels b { font-weight: 400; font-size: .95rem; line-height: 1.55; color: rgb(245 245 245 / .78); }

  .sp-twee-licht { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1.1fr); gap: clamp(1.5rem, 4vw, 3.5rem); align-items: center; }
  .sp-bord-t { display: grid; gap: 1rem; }
  .sp-def { display: grid; gap: .8rem; margin-top: .4rem; }
  .sp-def dt { font-family: var(--kop2); font-weight: 500; font-stretch: 125%; font-size: clamp(1rem, 1.6vw, 1.2rem); text-transform: uppercase; letter-spacing: -.01em; }
  .sp-def dd { margin: 0 0 .6rem; font-size: .92rem; }

  .sp-noot { font-size: .92rem; }
  /* Een lijnknop op inkt: `.knop-lijn` is voor de lichte panelen gemaakt. */
  .sp-knop-lijn { color: var(--vel); }
  .sp-knop-lijn:hover { background: var(--vel); color: var(--inkt); }

  .sp-plan { display: grid; }
  .sp-plan li { display: grid; grid-template-columns: auto minmax(0, 20rem) minmax(0, 32rem); align-items: baseline; gap: clamp(1rem, 3vw, 2.5rem); padding: clamp(1.1rem, 2.4vw, 1.8rem) 0; border-top: 1px solid var(--lijn); }
  .sp-plan li:last-child { border-bottom: 1px solid var(--lijn); }
  .sp-plan .nr { color: var(--inkt-2); }
  .sp-plan b { font-family: var(--kop2); font-weight: 500; font-stretch: 125%; font-size: clamp(1.05rem, 1.8vw, 1.45rem); text-transform: uppercase; letter-spacing: -.01em; }
  .sp-plan-voet { display: flex; flex-wrap: wrap; align-items: center; justify-content: space-between; gap: 1.2rem; margin-top: clamp(1.6rem, 3vw, 2.4rem); }
  .sp-plan-voet .lopend { font-size: .92rem; }

  .sp-eerlijk { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: clamp(1.2rem, 3vw, 2.5rem); }
  .sp-eerlijk li { display: grid; gap: .5rem; align-content: start; padding-top: 1rem; border-top: 1px solid var(--lijn); }
  .sp-eerlijk b { font-family: var(--kop2); font-weight: 500; font-stretch: 125%; font-size: clamp(1.05rem, 1.8vw, 1.3rem); text-transform: uppercase; letter-spacing: -.01em; }
  .sp-eerlijk .lopend { font-size: .92rem; }

  @media (max-width: 900px) {
    .sp-open, .sp-twee, .sp-twee-licht, .sp-eerlijk { grid-template-columns: 1fr; }
    .sp-plan li { grid-template-columns: auto 1fr; }
    .sp-plan li .lopend { grid-column: 2; }
  }
</style>
